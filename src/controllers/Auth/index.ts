import { SignInEndpoint } from "./sign_in.endpoint";
import { SignOutEndpoint } from "./sign_out.endpoint";
import { SignUPEndpoint } from "./sign_up.endpoint";
import { VerifyEndpoint } from "./verify.endpoint ";

export const AuthController = {
    SignInEndpoint,
    SignUPEndpoint,
    SignOutEndpoint,
    VerifyEndpoint
}